# Raw 입력 보관 위치

여기에는 원본 엑셀·SEC XML·원본 웹 응답처럼 다시 가공할 수 있는 최초 입력만 새 파일로 보관합니다. 현재 workspace에는 원본 파일이 없으므로 값을 추정해 채우지 않았습니다. 보유 여부와 출처는 [`source-register.json`](source-register.json)에 기록되어 있습니다.

파일명에는 가능하면 `datasetId_YYYY-MM-DD`를 포함하고, 새 수집본은 기존 파일을 덮어쓰지 않습니다.

